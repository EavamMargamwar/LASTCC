%{
#include <stdio.h>
int line_num = 1;
int found = 0;
%}

%%
.*\.com$	{
			found = 1;
			printf("\n Line %d: %s\n", line_num,yytext);
		}
.*\n	{ line_num++; }
.|\n	{ line_num++; }
%%

int main() {
	printf("Enter text lines:\n");
	yylex();
	if (!found) {
		printf("\n not found\n");
	}
	return 0;
}
